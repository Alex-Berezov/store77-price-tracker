{
  "name": "shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types and utilities for Store77 Price Tracker",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "test": "echo 'No tests yet'",
    "clean": "rimraf dist"
  },
  "dependencies": {},
  "devDependencies": {}
}
